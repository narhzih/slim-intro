<?php

namespace App\Controllers;

class Controller
{
    /**
     * Return view which will be
     * an instance of the twig view component
     * and make $data available to the view
     * @param $view
     * @param $data
     */
    protected function view($view, $data) {}
}